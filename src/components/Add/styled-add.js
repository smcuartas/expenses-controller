import styled from "styled-components";

const Add = styled.div`
    position:fixed;
    bottom:30px;
    right:30px;
    width:50px;
    height:50px;
    cursor:pointer;
    transition-duration:.3s;

    :hover{
        transform:scale(1.1);
    }
`

export default Add